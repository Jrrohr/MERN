import React from 'react';

const Header = (props) => {
    const headerStyle = {
        backgroundColor: '#6aa84f',
        height: '100px',
        width: '100%',
    };

    return (
        <div style={ headerStyle }>
        </div>
    );
}

export default Header;